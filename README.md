# oncepy

Async singleflight cache decorator for Python.

## Install dependencies

```bash
uv sync --group dev
```

## Quality checks

```bash
uv run ruff check .
uv run ty check
uv run pytest
uv build
```

